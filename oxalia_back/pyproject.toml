[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "B", "RUF"]

[tool.ruff.lint.flake8-bugbear]
# FastAPI's dependency injection intentionally uses calls in argument defaults.
extend-immutable-calls = ["fastapi.Depends", "app.core.deps.require_role"]
